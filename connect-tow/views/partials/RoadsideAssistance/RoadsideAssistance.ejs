<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redefining Roadside Assistance</title>
    <link rel="stylesheet" href="Roadside.css"> <!-- Link to your CSS file -->
</head>
<body>
    <div class="container">
        <div class="text-section">
            <h1>Redefining Roadside Assistance</h1>
            <p>
                What does that mean? It means we looked at this industry and realized
                the only way to provide truly exceptional, reliable roadside assistance
                was to completely rethink the technology backbone for these services.
            </p>
            <a href="#services" class="button">Learn More</a>
        </div>
        <div class="image-section">
            <img src="road.jpg" alt="Roadside Assistance App">
        </div>
    </div>

    <script>
        // Example JavaScript for smooth scrolling (optional)
        const learnMoreButton = document.querySelector('.button');

        learnMoreButton.addEventListener('click', (event) => {
            event.preventDefault(); // Prevent default anchor behavior
            const targetId = learnMoreButton.getAttribute('href'); // Get target ID
            const targetElement = document.querySelector(targetId); // Select the target element

            if (targetElement) {
                targetElement.scrollIntoView({ behavior: 'smooth' }); // Scroll to target
            }
        });
    </script>
</body>
</html>
